# Copy this to .env.local and fill in real values
# PAT must be base64-encoded to avoid GitHub secret scanning revocation
# Run: echo -n "github_pat_YOUR_TOKEN" | base64
VITE_GH_PAT_B64=your_base64_encoded_pat_here
VITE_GROK_API_KEY=your_groq_api_key_here
